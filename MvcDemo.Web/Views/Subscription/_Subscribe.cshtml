@model MvcDemo.Web.Models.Subscription.SubscribeModel

@if (Model.SubscriptionSaved)
{
    @Resources.GetString("TestMvcDemo.News.Subscribed");
}
else
{
    using (Ajax.BeginForm("Subscribe", "Subscription", null, new AjaxOptions
    {
        HttpMethod = "POST",
        InsertionMode = InsertionMode.Replace,
        UpdateTargetId = "SubscribeForm"
    }, new { id = "SubscribeForm" }))
    {
        @Html.AntiForgeryToken()
    
        @Html.ValidatedEditorFor(m => m.Email)
    
        <input type="submit" value="@Resources.GetString("TestMvcDemo.News.Subscribe")" class="btn btn-primary" />
    }
}